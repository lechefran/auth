// Package auth provides core authentication types and service wiring.
//
// The root package contains database-independent business logic. Storage,
// migrations, key management, and concrete database adapters live in focused
// packages so applications can choose the pieces that match their deployment.
package auth
